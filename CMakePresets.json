{
    "version": 3,
    "configurePresets": [
        { "name": "default", "binaryDir": "${sourceDir}/build" }
    ],
    "buildPresets": [
        {
            "name": "prod",
            "configurePreset": "default",
            "targets": ["MyGameProject"]
        }
    ],
    "testPresets": [{ "name": "ci", "configurePreset": "default" }]
}
